{
  "name": "trackvault",
  "version": "0.1.0",
  "private": true,
  "description": "A self-hosted music library server with admin and player web UIs.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/admin.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
